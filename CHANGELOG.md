# 更新日志

本文件记录 claude-in-dsh 的版本变化。遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 1.10.0-dsh015 — 2026-09-17

### 新增

- **模型目录有了设置界面**。以前只能手写 `~/.cache/ccmode/models.json`，现在在
  dsh 自己的设置页里（侧边栏「设置」→「模型目录」）就能增、改、删。这是 dsh
  官方的 `settings.section` 扩展点，不是自绘的对话框，所以导航、关闭、主题都
  是 dsh 自己的。

  同一份文件多了两个字段，都由这个界面维护：

  - `hidden` —— 被隐藏的模型不再出现在**任何**引擎的模型座里。内置模型不能删也
    不能改 id，隐藏是它们唯一的出路。隐藏只是不列出来，id 仍然算「认识」的：
    已经跑在这个模型上的会话不会因为一次隐藏就恢复不了。
  - `defaultCodexModel` —— Codex 引擎的默认模型，**和 `defaultModel` 分开**。
    空串（默认）＝跟随 codex 自己的 `config.toml`。两者共用一个槽位的话，在
    Codex 那边设一次就会顺手改掉新 Claude 会话开在哪个模型上。

- **写入是原子的**。原来是 `cat > models.json` 直接覆盖：写到一半插件热更新、
  或磁盘满，用户那份手写清单就没了。现在先写同目录的临时文件再 `mv`，最坏只
  留下一个 `.tmp`。

- 校验收敛在 host 一侧、写入之前：id 非空且字符合法、不与内置或已有 id 重复、
  `reasoning` 必须是布尔、设默认值必须指向清单里真有的模型、Codex 不接受
  `claude-*`（必然 401）。前端不重演这些规则，只负责把 host 的报错原样显示出来。

- **引擎注册表**（设置 →「引擎」）。三个引擎的显示名以前写死在客户端，现在能改，
  并且各带一个来源标记（本地 / 远程），存在 `~/.cache/ccmode/agents.json`：

  ```json
  { "claude": { "name": "Claude（网关）", "source": "remote" } }
  ```

  改完立刻生效：引擎座、模型座的分组标题、会话徽章 tooltip、几个座位自己的
  tooltip 都跟着改口（徽章的 ⌘ / ✳ 符号不变）。缺项、类型不对、写坏的整份文件
  一律取出厂值——一份写坏的名字不该让引擎座空掉。

  **它只改显示，不动执行层**：能被执行的引擎仍然只有 `dsh` / `claude` / `codex`
  三个（host 的 `isEngineId` 语义不变），注册一个第四种 id 会被拒绝——没有对应的
  执行路径，注册得进去只会让人以为能用。来源标记眼下也是注解而非开关：三个引擎
  都由本机 CLI 执行，标成「远程」只是用户自己的说明。

### 修复

- **`defaultModel` 从设置界面改完不生效**。它有两个落点：models.json（部署级
  默认）和 state.json 的 `#defaults`（用户上次显式选过的东西）。启动时后者覆盖
  前者，所以只写文件的话，下次重启会被那份更旧的显式选择盖回去 —— 现在两个
  都写。

## 1.9.0-dsh015 — 2026-09-17

### 新增

- **Codex 引擎**。第三个引擎，和 Claude 那条路完全不同：没有常驻进程、没有 broker，
  每一轮就是一次 `codex exec`（首发）或 `codex exec resume <thread>`（之后每一轮），
  stdout 是 JSONL 事件流。对话存在 Codex 自己的 thread store 里，插件只持久化
  thread id。会话日志依然由 dsh 的词汇写成，所以 UI 渲染是原生的：工具调用是 dsh
  自己的 Bash 卡片，错误在转录里可见且**不锁会话**（换回能用的模型即可在同一
  thread 上续接）。侧边栏徽章 ⌘ 与 Claude 的 ✳ 可区分。

  三条与 Claude 不同的约束，都已实测：
  - `resume` 不认 `-s/--sandbox` 也不认 `-C/--cd`；沙箱走 `-c sandbox_mode=…`，
    cwd 由 spawn 决定（resume 按 cwd 过滤 thread，所以 cwd 必须设对）。
  - Codex 没有 token 级增量，每轮重发完整历史 —— token 成本是架构代价，不是 bug。
  - Codex 的 provider 不认 `claude-*`，请求必然 401（实测）。

### 修复

- **写坏会话的 `stream` 层级 bug**（本分支此前引入）。契约要求 `stream` 是**事件层**
  字段，代码却写在 `message` 里 —— 写入时毫无异常，直到重新打开该会话，投影拿
  `data.stream` 去迭代直接抛 `stream is not iterable`，而 dsh 的日志只追加，**写坏一次
  这个会话就再也打不开**。三处写入点（Claude 的 `note()`、Codex 的 `note()`、
  Claude/Codex 共用的失败提示）一并修正，并补了回归测试。

  已损坏的会话（`session-ded1a36e`、`session-cebff2fb`）留在归档里，不可恢复 ——
  写入侧只能防新增。

- **模型默认值按引擎分开**。此前 `defaults.model` 是单一全局槽位，而新会话的初值
  不看引擎，于是：Codex 会话里选个模型会改掉新 Claude 会话开在哪个模型；更糟的是
  还原状态文件那条路没有引擎校验，`mode: codex` + `model: claude-opus-5` 这种组合
  能开局 401。现在两个引擎各有一个槽位（`model` / `codexModel`，后者空串＝跟随
  codex 自己的 config.toml），`stateOf` 按引擎取，`rememberDefaults` 只写当前引擎
  那个槽位，切引擎时载入目标引擎记着的默认。老状态文件带迁移，升级不丢已选模型。

- **引擎适用性校验收敛到一处**。原先三处（`model.set` / `restoreStates` /
  `loadPersistedStates`）各自判断，其中 `restoreStates` 漏了 —— 状态文件里的
  「Codex 会话 + claude-*」能绕过防御层。现统一走 `modelFitsEngine()`。

- **下拉菜单不贴触发器、且被视口底部切掉**。菜单借用 dsh 自己的 `menu` class
  （`position: fixed`），但没跑 dsh 那套定位 JS，fixed 无偏移就落回**静态位置**：
  实测菜单横向偏出 390px、底边 769 超出视口 720 共 49px（输入框本来就在屏幕最
  下面，向下开必然被切）。`position: absolute` 也救不了 —— 外层 `scrollBody` 是
  `overflow: auto`，会被滚动容器裁掉。现新增 `useAnchoredMenu()`：照 dsh 的做法
  fixed + 现算坐标，**下面放不下就翻到上面**，横向贴住触发器且不探出视口。两处
  菜单（通用 Select 与模型座）共用，失效的 `.ccmode-menu-left` 移除。

- **模型座分组标题写死「Claude Code」**。Codex 会话里那份清单本来就是它自己的
  模型，标题改成跟引擎走。

- **徽章不重绘**：会话切到 Codex 后仍戴 Claude 的 ✳，现改为存在时也更新。

## 1.8.3-dsh015 — 2026-09-17

### 修复

- **自定义模型「能看见但选不上」**。1.8.2 把自定义模型加进了 `catalog`，但
  `model.set` 等 **6 处**校验仍在查硬编码的 `MODELS` 常量，自定义 id 一律被判非法、
  静默丢弃。现改为统一的 `isKnownModel()`（读文件后维护的 id 缓存），并在插件启动时
  预热缓存 —— 顺序反了会让恢复会话状态时把自定义模型当非法值丢掉。

- **`defaultModel` 不生效**。此前只写进 `catalog`，而 `catalog.defaultModel` 仅用于
  客户端首帧的临时值；真正决定新会话模型的是 host 端持久化的 `defaults`。现在
  `loadPersistedStates()` 会先用它播种 `defaults.model`（持久化的用户选择优先级更高）。

### 变更

- `models.json` 现在支持三种写法：
  - `[ {...} ]` —— 只加模型
  - `{ "models": [...], "defaultModel": "..." }` —— 两者都配
  - `{ "defaultModel": "..." }` —— 只改默认

### 验证

选中 `deepseek-v4.1-flash` 后 chip 立即更新，且实际启动参数为
`claude -p ... --model deepseek-v4.1-flash`，模型正常响应（6 秒，101 tok）。

## 1.8.2-dsh015 — 2026-09-16

### 修复

- **下拉菜单不贴 trigger，跑到屏幕最左边**。dsh 的 `_7KE1Ra_menu` 是 `position:fixed`
  （它的 JS 用 `getBoundingClientRect()` 设 inline `top`/`left`），本插件复用了该 class
  但没跑那套 JS。原规则 `.ccmode-menu-left { left:0 }` 对 fixed 元素的包含块是**视口**，
  于是菜单被推到屏幕最左。改为 `position:absolute` 相对 `SEL.root`（本就是
  `position:relative`）并 `top:calc(100% + 4px)`，菜单即锚定在 trigger 正下方左对齐。
  影响引擎选择器与权限选择器两处（都用 `align:'left'`）。

### 新增

- **支持自定义模型**。Claude 引擎模式下模型座原本只列写死的 7 个 Claude 模型；
  但如果 `claude` CLI 走自建网关（`ANTHROPIC_BASE_URL`），`--model` 的值是**原样透传**的，
  任何网关认识的模型名都能用。现在把额外模型写进 `~/.cache/ccmode/models.json` 即可：

  ```json
  [
    { "id": "deepseek-v4.1-flash", "name": "DeepSeek V4.1 Flash", "reasoning": true }
  ]
  ```

  同 id 以内置为准（不覆盖）；文件缺失或 JSON 非法时静默回退到内置清单。
  也支持 `{ "defaultModel": "..." }` 覆盖新会话的默认模型。

  `catalog` RPC 因此改为 async。

## 1.8.1-dsh015 — 2026-09-16

### 修复

- **兼容 DSH `0.1.5-rc.1`**（fork 独有，上游 1.8.0 无此修复）。上游版本在 DSH 0.1.5 上可用加载，
  但一发消息即失败：`session.events is not iterable`。逐条排查后发现是 **4 个不兼容点**：

  1. **`session.events` 属性已移除**（15 处）→ 改用 `session.snapshotEvents()`。
     DSH 把读日志从属性改成了方法（`snapshotEvents()` / `ownEvents()` / `eventAt()`），
     而 `append` 保留了 —— 所以写入路径不炸，**只有第一次读日志时才暴露**。

  2. **`tool/code-dispatch` / `tool/code-dispatch-start` 改名**（2 处）→ `tool/ptc-dispatch` / `tool/ptc-dispatch-start`。

  3. **`assistant/chunk` 事件类型已移除** → 不再为每个 chunk 写独立事件，
     改为累积成**压缩记录**内嵌在 `assistant/message.stream` 里。

  4. **`assistant/message` 不再接受 `sourceEventSeqs`**（1 处）→ 该事件自己就内嵌了 source stream，
     携带它会被直接拒绝（`assistant/message embeds its source stream and cannot carry sourceEventSeqs`）。

  3 与 4 是同一个变更：DSH 0.1.5 把流式传输从「事件序列」换成了「压缩记录内嵌」。
  压缩格式见 `@deepseek-ai/dsh-llm` 的 `AssistantStreamRecord`；官方实现是 `AssistantStreamAccumulator`，
  本 fork 按其语义内联实现（插件是零 import 的纯自包含设计，不能直接 import）。

### 验证

- 本机 DSH `0.1.5-rc.1` 实测：会话切到 Claude Code 引擎后可正常多轮对话，
  转录/工具卡片/审批走 dsh 原生渲染。
- 确认走的是**真实 Claude Code CLI**：`/opt/homebrew/bin/claude -p --input-format stream-json ... --model claude-opus-5`，
  因此会加载 `~/.claude` 下的全部配置（skill / CLAUDE.md 等）。
- `pnpm build` 从修好的 `src/` 生成的 `lib/index.js` 与手工修复版**字节一致**（双向验证）。

## 1.8.0 — 2026-08-25

### 新增

- **新对话开在上一次选定的那套配置上**：引擎（DSH / Claude Code）、Claude 的权限档、模型，以及 effort。此前每个新会话都从硬编码的「DSH + 监督 + 默认模型」开始，于是每开一个新对话都要把同样的三个选择重做一遍。

  写这份默认的只有三个入口——引擎选择器、权限档选择器、模型选择器——所以它记的是「有人主动选了什么」，而不是「某个会话恰好继承了什么」，否则默认会被自己的传播不断确认、再也回不去。已经存在的会话完全不受影响：默认只决定一个还没有任何设置的会话如何开局，而一个已经有内容的会话，引擎仍然由 `committedEngine` 从会话日志判定，永远压过这份默认。

  存在插件状态文件的 `#defaults` 段里（`~/.cache/ccmode/state.json`，与每会话设置同一处；dsh 没有给插件用的运行时配置写入口）。读回时逐字段校验：菜单里删掉过的选项，不该让此后每个新对话都停在一个选择器已经显示不出来的值上。旧的状态文件没有这一段，出厂值照旧是 DSH + 监督 + 默认模型，第一次改选择时才开始记。

## 1.7.0 — 2026-08-25

### 新增

- **「在新对话中分支」现在真的把 Claude 一起分支了**。dsh 的 fork 只做一件事：把源会话的事件切片当种子建一个新会话，并在 header 里记下 `parentSession`。它不认识这个插件，而 `claudeSessionId` 存在插件自己的 state 里、以 dsh 的 sessionId 为键，于是不会跟过去。更麻烦的是引擎归属是从**会话日志**判定的（`committedEngine` 扫到 provider 为 Claude 的 assistant 消息就算数），而那些事件被复制过去了——所以 fork 出来的会话仍然显示成 Claude 引擎、屏幕上留着一整段历史，第一次发消息时却起了一个全新的 Claude。实测：父会话里问「你回答 A」「你回答B」都答了，fork 出来的会话第一句「你回答c」，Claude 自己说「我这边没有任何上下文——没有前面的提问，也没有 A/B/C 选项列表」。

  现在没有可 resume 的 id 时，会先看 `session.header.parentSession`：父会话若由 Claude 驱动且它的 transcript 还在磁盘上，就用 `--resume <父> --fork-session --session-id <新 id>` 启动。三个开关可以同用（对着 CLI 实测过：子会话用的正是我们指定的 id，并且答得出父会话里存的暗号），所以新 id 由插件分配，`meta.json` 和持久状态不必等 init 握手才知道自己是谁。父会话不是 Claude 会话、没有记录 id、或者 transcript 已经不在了，都退回今天的行为（起一个全新的 Claude）——而不是让 `--resume` 指向一个不存在的会话，那会让 CLI 直接退出，而且 id 是持久化的，那一轮会永远死在同一处。

- **会话标题不再是一句被截断的开场白**。这件事有两层，都修了。

  其一，Claude 自己起的名字写在它的 transcript 里（`ai-title` 行），**不在流里**——三个 broker 的 `out.log` 里一个 `ai-title` 事件都没有，只能读文件。读的时候每一行都要真正解析并核对 `type`，不能只靠 grep：一个正在讨论标题同步的会话，会把 `{"type":"ai-title"…}` 原样写进自己的工具输出和正文，只匹配字符串就会把它自己的聊天当成标题（查这件事时先被这样骗过一次）。

  其二，更要紧的是 Claude 多数时候根本不起名——44 个由本插件驱动的 Claude 会话里只有 3 个有 `ai-title`，那是交互式 UI 的行为。而 dsh 自己的标题生成对 Claude 会话是**结构性失效**的：`SessionTitleService` 在用户消息上把任务标成 pending，真正启动却要靠 `request/header`（Claude 会话从不写）或 dsh 自己那次 loop 请求（Claude 会话不走 dsh 的模型）——两个触发源都不会来，于是标题永远停在 fallback。所以每轮之后：Claude 起过名就用它的（经 `rename`，顺带取消 dsh 那次多余的标题生成），没起过而标题还是 fallback 就调一次 `sessionTitle.refresh`，请 dsh 用自己的模型起个正经名字，每个会话只请一次。

  人手改过的标题一概不动，永久：插件记得自己上次写进去的是什么，座位上换了别的内容就说明是人改的，从此收手（`refresh` 的文档明说它会覆盖用户标题，所以这道闸必须在它前面）。

## 1.6.2 — 2026-08-25

### 修复

- **1.6.1 的读取器根本不会退出**。它只在收到 `result` 且没有后台任务时才起那个静默计时，可它恰恰是在主循环消费掉这一轮的 `result` **之后**才启动的——面对的是一个空队列，而下一个 `result` 只有在 Claude 自发开一轮时才会来。于是一场寻常对话结束后，读取器就永远挂在那儿等一条不会来的消息，`run` 一直钉在 `runs` 里，而 `reapIdleBrokers` 对任何仍被本插件 attach 的会话直接 `continue`。1.6.1 想修的是「自转的会话收不掉」，实际却把它扩大成了「**所有**会话都收不掉」——在这台 2 GB 的机器上这比原来的毛病更糟。

  现在退出判据只有一条：**静默**。计时在读取器启动时就武装好，之后每收到一条消息重新计时。有没有后台任务不再决定去留，只决定静默要多久才算数——没有是 10 秒，有是 30 分钟（后台的 `sleep` 期间本就一片安静，按 10 秒走会正好错过它醒来那一轮；30 分钟又短于 reaper 的 45 分钟，所以被彻底遗忘的会话仍然收得掉）。

- **接手时不知道后台还有任务在跑**。那个计数来自 `background_tasks_changed`，而这条事件通常在读取器存在之前就滚过去了：要么被上一轮自己消费掉，要么发生在 dsh 重启之前。从零开始等于在最需要长窗口的场合选了短窗口。现在读取器启动时先从 `out.log` 尾部把最后一次的计数读回来。

### 新增

- **打开会话就重新跟上还活着的 Claude**。读取器只活在插件的生命周期里：dsh 重启、插件更新、它自己静默超时让出，都会留下一个 broker 抱着仍在运行的 Claude 而没人在读。`drainDetachedOutput` 补不了这个——它投影已经跑完的轮次，然后就结束了，Claude 在那之后说的话仍然没人听见。现在 `state.get` 会在补齐之后再 attach 上去接着跟（顺序执行，不是并行：两者读同一个 offset，从补齐之前的位置起跟会把刚投影的内容重放一遍）。

  它绝不启动进程——broker 不在就什么都不做——而且判错的代价为零：没有流量的话读取器会自己超时并 detach。

## 1.6.1 — 2026-08-25

### 修复

- **后台任务跑完之后，Claude 自己开的那一轮在 dsh 里完全看不见**。Claude Code 的一轮以 `result` 收尾，但 Claude 不一定就此打住：一个后台任务（`Bash` 带 `run_in_background`、`Monitor`、默认走后台的 `Agent`）完成时，CLI 会自发开一轮——`task_notification` → **`init`** → assistant → `result`——而 dsh 这边没有任何轮次是为它开的。此前主循环在第一个 `result` 上就 `return` 了，这条流从此没人读。实测一个等 MCP 恢复的会话里，前端停在 12:33 的「等 3 分钟计时结束再试」，Claude 却一路跑到 12:42 又说了两轮，一个字都没到前端。

  代价是双份的。其一，读取端（`tail -f` 的 pump）并没有停，消息继续往 `run.queue` 里堆而没有消费者——那个会话滞留了 345 条、169 KB，并且还在涨；这是一处无界的内存泄漏，而这台机器上每个挂着的 Claude 进程本来就已经占着约 130 MB。其二，队列是条没有轮次归属的裸 FIFO，下一次真正发消息时会先把这批陈旧消息当成**这一轮的回答**渲染出来，再撞上那个陈旧的 `result` 提前收尾，把真正的回答挤到再下一轮——错位会一轮一轮传下去。

  现在轮次之间保留一个读取器，但它**不持有内容**：每条消息分类后即丢，一轮走完就按字节区间直接从 broker 的 `out.log` 里把这一段读出来投影成历史（不调模型、不耗额度）。区间的端点由 `result` 自己带着——pump 是整块 chunk 同步排干的，等消息交到手上时它往往已经读过了这一轮的结尾、进到了下一轮，所以不能用「pump 读到哪」来当终点。每个会话的常驻开销因此是一个挂起的闭包加两个数字。

  它自己知道什么时候该走：`background_tasks_changed` 每次都带完整任务列表，列表空了就说明再没有东西能唤醒 Claude，此时起一个 10 秒静默窗口，期间没有新消息就让出并 detach（自发的下一轮里 `init` 紧跟在 `result` 后一两行，这个窗口只需盖住那个间隙）。让出之后 `runs` 里不再有它，原有的 `drainDetachedOutput` 守卫自然放行，从 `attach.json` 的 offset 接着补——所以万一让早了，最坏也只是延后到你打开会话时才看到，不会丢。

  两个连带修复：自发轮次里的权限请求现在照样送去 dsh 的审批卡片，否则 Claude 会卡在一个没人看得见的问题上，连带把进程和它的内存一直占着；以及这类会话的进程终于能被回收了——`reapIdleBrokers` 按 `out.log` 的 mtime 判空闲，而一个无休止自转的 Claude 会一直刷新它，45 分钟的空闲判据永远不成立。

  用户在 Claude 自言自语到一半时插话也有交代：读取器让位前会把这半轮照原样投影出去（投影本来就会给没等到结果的调用补一条结果），并清空队列，免得那半轮的 `tool_result` 落进新一轮变成找不到调用的孤儿。

## 1.6.0 — 2026-08-23

### 变更

- **模型选择器里只留具体模型**，去掉了「跟随 Claude 自己的设置」那一行。它在新建对话里显示成「Claude 默认」——这话没错，可它恰恰说不出那到底是哪个模型（取决于 CLI 自己的配置），而这个座位存在的意义就是回答这个问题。新会话现在直接落在 `claude-opus-5` 上，开局就写着它自己的名字，进程也带着 `--model` 启动。
  已经存了空选择的老会话保持不动：它们的进程本来就跑在 Claude 自己的配置上，菜单少一行不该悄悄把一个进行中的对话换到别的模型上去；那种会话在进程报出路由前显示「Claude 自己的设置」，报出后就显示实际路由。导入的对话仍然采用它 transcript 里记录的模型（此前那条修复的判定是「没选过模型」，默认值变成具体模型后这条几乎永不成立，已改为「还停在默认值上」）。

## 1.5.3 — 2026-08-23

### 修复

- **上下文面板的三行分解对不上自己的总数**。环上的读数是准的（宿主发布窗口、分子取 Claude 报的 usage），但点开后的分解是 dsh 从**它自己的**会话日志算的：系统提示词与工具两行来自 `request/header`，而 Claude 会话永远不写这个事件（它就是会话的模型派发，写了会把输入框搞坏）；对话消息那行则是给 dsh 的 surface 估价，而那不是 Claude 实际带着的上下文（没有记忆文件、没有技能、没有 MCP schema，思考块也只是摘要）。于是面板显示「系统提示词 ~0 / 工具 ~0 / 对话消息 ~71.5K」，而它头上的环正确地写着 192K——三行加起来不到总数的四成。
  现在宿主问 Claude 自己的 `/context` 要这份真实构成（不花模型调用），按 Claude 拼请求的方式折到 dsh 的三行上：记忆文件与技能本就属于系统提示词块，MCP schema 属于工具，其余是对话。数字写进 dsh **自己的**节点——同样的行、标签、色块、条形图；只有三个数值和条形图内部的比例变了，填充长度仍是环上那个精确百分比。dsh 若把它按 0 宽丢掉的分段省掉（正是这个面板的常态），缺的段按图例色块上的类名照原样补回。面板结构一旦不是预期的样子（换了标记、换了行数），整块逻辑就什么都不碰。
  两点实测细节都进了测试：报告里 **deferred 的工具行不算占用**（报告自己的算术能证明：非 deferred 各行之和恰好等于「窗口 − 空闲」，把 deferred 算进去会多 24.4K），以及报告后面那几张明细表（MCP Tools / Memory Files / Skills）**不参与统计**——它们是同一批 token 的展开，解析范围因此收紧到「按类别估算」那一节。
- **一次 `ctx.timeout` 就能打断正在跑的一轮**。cordis 的定时器属于插件的 fiber，插件热更新或上下文被销毁后再碰它会抛 `cannot get property "timeout" without inject`——实测表现为连着三条 `写入 Claude 失败` 然后这一轮死掉。这些地方要的只是一个定时器，没必要跟着 fiber 一起死：现在统一走一个会退回宿主原生 `setTimeout` 的守卫（写管道超时、启动轮询、取消后的兜底 kill、每一次文件读取的看门狗、状态落盘的合并计时都在内）。
- **`/context` 探测每轮刷新**。此前窗口一测到就不再问，而构成是每轮都在变的——面板于是永远显示上一轮甚至第一轮的分解。现在窗口仍按模型选择缓存（它不换模型不会变），构成每轮结束后刷新，面板打开时若已过期还会在当前数字背后再取一次。
- `dsh.plugin.json` 的版本号补回同步（一直停在 1.5.0）。

## 1.5.2 — 2026-08-22

### 修复

- **记录在案的 Claude 会话 id 没有对应 transcript 时，第一轮永远死于 `error_during_execution`**。启动命令无条件 `--resume <id>`，而 CLI 对不存在的会话直接报 `No conversation found with session ID: …` 然后退出；id 又是持久化的，于是每一轮都撞死在同一处。这种「幽灵 id」是真实存在的状态：1.5.1 之前的 macOS 启动把 id 先持久化了，broker 却从未跑起来、transcript 从未写过。现在 resume 之前先确认 `~/.claude/projects/<slug>/<id>.jsonl` 真在磁盘上，不在（或为空）就换一个**新 id** 全新开始——不复用死 id，避免与其他项目目录下的同名 transcript 相撞。旧的中毒会话由此自愈，无需手动清理。

## 1.5.1 — 2026-08-22

### 修复

- **macOS 上第一条消息就死于「写入 Claude 的输入管道超时（10 秒）」**。broker 的启动命令用了 `setsid nohup node …`，而 `setsid(1)` 是 util-linux 独有的，macOS 根本没有——broker 从未启动，fifo 没有读端，`cat > in` 卡在 open 上直到超时。现在改用 node 自己的 `spawn(..., {detached: true})`（在所有 POSIX 平台上就是 setsid(2)）把 broker 脱进独立会话，并且用 dsh 自己的 node（`process.execPath`）而不是 PATH 上碰运气找的那个——pm2/GUI 环境的 PATH 里常常没有 node。启动失败也不再沉默：5 秒内 broker 没就位就直接报错并提示看 `cc-mode [launch]` 日志，而不是十秒后一条不知所云的管道超时。
- **所有 `stat -c` 换成 GNU/BSD 双兼容**（`stat -c … || stat -f …`）。BSD stat 不认 `-c`，此前在 macOS 上文件大小恒为 0（崩溃恢复的 offset 兜底会从头重放整段 broker 日志）、「导入 Claude Code 对话」列表恒为空、闲置 broker 永不回收。bundle 测试新增回归防线：出现无兜底的 `stat -c` 或 setsid 启动命令即失败。
- **detach/checkpoint 记录的字节偏移可能含半行，重连丢一整行**。tail 的 chunk 边界可以落在行中间，而 `run.offset` 按 chunk 累加；把它写进 attach.json 后，下次 attach 从行中间续读，开头的残片解析失败被丢弃——那一整行（可能是一条 assistant 消息或 result）就没了。现在另记一个「最后完整行」偏移，checkpoint 与 detach 都用它。
- **一次性命令与轮次抢同一条流的竞态**。`activeTurns` 和 borrow 标记都在 await 之后才落位：上一轮 finally 里踢出的 `/context` 探测和用户紧接着发的下一条消息能互相穿过对方的检查，然后两个消费者平分同一个消息队列——命令循环还会吞掉它不该看见的 `control_request`（权限请求就此无人应答）。现在两边都走同一把**同步占位**的会话锁，排队顺序在任何 yield 之前就定死。
- **steering 路径漏了 carrier 守卫**。窄竞态下投影唤醒用的 NUL 标记文本会被当成插话发给 Claude，且 `carriersInFlight` 永不清除——之后所有断档补播/导入镜像都不再被唤醒。现在 claim 到 carrier 直接跳过并清标记，内容照常在下一轮渲染。
- **管道写超时后残留的半行不再污染下一条消息**。超时被 terminate 的 `cat` 可能已写入半条 JSON（带图片的消息有几 MB）；下一次写入先补一个换行，把残片变成 CLI 直接丢弃的一条坏行，而不是让它粘在新消息前面把两条都毁掉。
- **面板里跑 `/clear` 后 resume 映射指向旧会话**。一次性命令循环收到 `init` 握手时此前不更新 `claudeSessionId`；`/clear` 给进程换了新会话 id，持久化的续聊映射却还指着清空前的那段。

### 新增

- **后台任务在转录里可见**。Agent 工具现在默认后台跑，detached Bash / Monitor 同理——它们的 `system` 流事件（`task_started` / `task_notification`）此前被整族丢弃，模型稍后「凭空」接话在 dsh 里毫无来由。现在任务启动与完成通知（含子 agent 的最终报告）作为插件注记进入转录。
- **可选服务晚到时补绑定**。`approval` / `userQuestions` / `attachments` / `agents` 此前只在插件加载时一次性 `ctx.get`，加载顺序靠后就静默降级（提问退化回权限卡片、图片存不了）。现在启动后约 20 秒内重试解析，绑上为止。

## 1.5.0 — 2026-08-21

### 新增

- **工具结果里的图片进 dsh 的附件通路**。Claude 读一张 PNG（`Read` 一个图片路径、或 MCP 工具返回图片）时，结果是一块内联 base64 的 `image` 内容块，而投影只把非文本块拍成 `[image]` 一行占位符——转录里看得到"读了张图"，看不到图。现在这些字节在投影之前先经 dsh 的 `attachments.saveImage` 落成持久附件，写进会话日志的是 `{type:'image', attachment}`，也就是 dsh 自己的 `read_image` 用的同一种块。于是历史图片的原生能力（缩略图、点击开原图灯箱、导出）对 Claude 的结果一样成立。提交失败（附件服务缺席、超限、格式被拒）时仍退回原来的占位行，结果本身照常进转录。

## 1.4.0 — 2026-08-21

### 新增

- **Claude 的 AskUserQuestion 走 dsh 原生提问卡片**。此前它只是一次普通的权限请求：审批卡片上问你"要不要放行 AskUserQuestion"，你点允许，Claude 收到的却是 `The user did not answer the questions.`——问题本身你根本没看见，答案也无从谈起。现在它被识别出来后交给 dsh 的 `userQuestions` 服务，用的就是 `ask_user_question` 那张卡片：选项、多选、"其他"自由文本一样能用，答案按题面文本回填进 `updatedInput.answers`，Claude 那边读到的是 `Your questions have been answered: "…"="…"`。
  提问一律问人，不看权限档——权限档说的是"工具要不要拦"，而在 dsh 里提问从来不是可以替人代答的东西；完全放行模式下自动允许换来的只是一句"没有回答"。没有应答者、被中断、或是被委派的子 agent 发起的提问，都归到"没人回答"这一个结果上，模型照常继续，不会把这一轮吊死。

- **Claude 的计划模式审批走 dsh 原生计划审核卡片**。ExitPlanMode 同样从权限通道来，此前也只是被问成"要不要放行 ExitPlanMode"。现在它交给 `userQuestions`，用的是和 dsh 自己的 `exit_plan_mode` **逐字相同**的题目 id、选项标签（Approve / Keep planning）与 `intent: {kind:'plan-review'}`——客户端据此渲染的就是同一张计划审核卡片，计划正文走 `detail`。批准判定也照抄：只有恰好选中 Approve 且没有附带自由文本才算批准，写了字就是反馈、等于继续规划，反馈原样成为工具结果送回模型（实测模型收到的就是那句话，且仍留在计划模式）。
  批准时顺带把该会话的权限档从「计划模式」落回「监督」：姿态在这边是启动参数，不改的话 broker 换进程后 `--permission-mode plan` 会把一个已经批准过计划的会话又按回只读。（输入框里的档位选择器要等下次轮询/重挂才会跟着变。）

## 1.3.6 — 2026-08-20

### 修复

- **稍大一点的图片粘贴不进去**（`500 · Image exceeds the configured per-side pixel limit`）。dsh 的附件库默认拒绝任何一边超过 **2000px**、或超过 3.5MB 的图片，而一张 Retina 截图轻松就越过这两条线；dsh 自己的输入框只会弹个 toast，等于截图根本发不出去。现在粘贴时先按 dsh 实际配置的上限（宿主新增 `image.limits` 读取 `attachments.imageLimits`）等比缩到能过，必要时转成 JPEG 再逐级降尺寸直到满足字节上限。缩小对 Claude 没有损失——它本来就会把长边超过 ~1568px 的图再缩一遍。

## 1.3.5 — 2026-08-20

### 变更

- **排队的插队消息带着它自己的图片**。跑轮次时按普通 Enter，消息进 dsh 的排队区等下一轮，而图片此前留在会话级暂存里：输入框上方一直挂着缩略图，排队行里却看不出这条消息带图，只有 Cmd/Ctrl+Enter 立即插队才会把图片带走。现在消息一进排队区，图片就绑到**那条消息**上——dsh 自己的排队行按原生规则预览成 `[image]`（和 dsh 原版带图的排队消息一样），输入框上方随即清空，轮到它时图片跟着它一起发给 Claude。

### 修复

- **图片发送后缩略图要过 1–2 秒才消失**。此前只靠 1.2 秒一次的轮询发现暂存被取走。现在按下 Enter（或点发送）本身就是一个信号，立刻连查几次。

## 1.3.4 — 2026-08-20

### 修复

- **工具调用的结果会被丢掉，卡片停在「这次调用没有等到结果（轮次被中断）」**。步骤边界会把该步骤里还没回结果的调用一律判成"等不到了"——可 Claude 开下一条助手消息（也就是下一个步骤）时，上一个调用**正在跑**是再正常不过的事，并行调用尤其如此。于是那个还活着的调用被当场标红，一秒后真正的结果回来又被当成重复丢弃。实证：`toolu_01HYfe…` 的 `tool/result` 里存的就是那句中断提示，而那条 Bash 其实跑完了。现在调用归**这一轮**管而不是归某个步骤管，只有轮次真正结束时才给没回结果的调用补一条中断说明。
- **dsh 关闭时插件会主动打断 Claude**。dsh 停机会中止所有在跑的轮次，而插件把这个中止一律当成"用户按了停止"，于是给 Claude 发 `interrupt`——正好把 broker 存在的意义抵消掉：轮次当场死掉，Claude 的转录里留下一串 `[Request interrupted by user for tool use]`，重启后的补播自然什么都捞不回来，因为根本没有内容可捞。现在停机信号一到就先记下来（dsh 自己也监听 SIGTERM/SIGINT，加这一个不改变任何终止行为），中止发生在停机过程中就不打断，让 broker 把这一轮跑完。

## 1.3.3 — 2026-08-20

### 修复

- **补播的内容渲染不出来：一轮里的第二批投影会把步骤号从 1 重来**。载体轮次会把排队的**每一批**内容都渲染进同一轮，而每批都自己从 step 1 开始编号。实证（session-07071e1a）：第一批写了 `turn 3 / step 1…9`，第二批又写了一个 `turn 3 / step 1`——客户端遇到倒退的步骤号就丢掉后面的内容，界面上就是「（dsh 重启期间 Claude 继续完成了这一轮，以下是断档期间的内容。）」后面**什么都没有**。现在步骤号接着这一轮已有的最大值往下走。
- **载体消息会卡在输入框上方的排队区里**。此前每排一批内容就发一条载体消息唤醒 dsh；如果这时已经有一轮在跑，dsh 就把它放进「下一轮」队列，而没有人会去开那一轮——于是一条 `claude-in-dsh:render-pending` 就明晃晃地挂在输入框上方，直到你恰好发了条消息为止，那批内容也一直不渲染。现在同一会话同时只允许一条在途载体，且只在没有轮次在跑时才发；轮次结束时再检查一次队列。

## 1.3.2 — 2026-08-20

### 新增

- **侧栏对话行的 ⋯ 菜单里多一项「复制 Claude 会话 UUID」**（只在 Claude 对话上出现）。复制的就是 `claude --resume <uuid>` 后面那个 id，可以直接在终端接着聊同一段对话。冷会话也能复制——此前 `state.get` 只在进程活着时才报这个 id，现在会回落到持久化的记录。

### 修复

- **上下文占用的分母是猜的，而且猜错了**。窗口大小此前从模型名推断（`[1m]` 后缀 → 100 万，否则 20 万），但 Claude Code 的握手回的是**你给它的那个 id**：带 `--model claude-opus-5` 启动就报 `claude-opus-5`，同一套装置不指定模型则报 `claude-opus-5[1m]`——同一个进程、同一个 100 万窗口、两个不同的字符串。于是这个对话在实际占用 243k（约 24%）时被报成 **100% / 200K**。现在改成直接问 CLI：`/context` 会明说「Tokens: 19.8k / 1m」，带外执行、不调用模型，按模型选择缓存并持久化。
- **重启后接回老进程会丢掉模型路由**。`system/init` 只在新进程启动时来一次，而 dsh 重启走的是「接回 broker 里还活着的 Claude」那条路，于是路由为空、窗口无从判断。现在接回时从 broker 自己的输出日志里把最后一条握手捞回来。
- **上下文窗口只在握手时发布一次**。同样因为接回不带握手，长期跑着的对话——最需要看占用的那种——反而永远亮不了圆环。改成每轮开始都发布一次。

## 1.3.1 — 2026-08-20

### 修复

- **并行工具调用会毁掉整段渲染**。同一条助手消息里发出的两个 Bash 调用，CLI 会先单独回一条先跑完的结果，随后又把**完整的** tool_result 数组（含那条已经回过的）再发一次。插件照单全收，于是同一个调用在日志里出现两条 `tool/result`；dsh 的会话节点把"一个调用两个结果"当协议错误——先闪一行红字，那张卡片和它后面的内容就再也画不出来了。现在一个调用只写一条结果，谁写都算数（实测日志里已出现 4 处这种重复）。
- **`/compact` 一定失败**。一次性命令的等待上限是 90 秒，而 `/compact` 要把整段对话交给模型重写：实测一个只有 23k token 的会话就花了 **44 秒**，真实长会话是几分钟起。到点后面板报「命令 90 秒没有返回结果」，可后台其实压缩得好好的。现在命令改成后台作业、面板轮询，上限 30 分钟，并显示 CLI 自己的状态（「正在压缩上下文… 44s」）。
- **`/compact` 成功了也说"没有输出"**。它的 `result` 是空的，真正的数字在 `compact_boundary` 事件里。现在按终端的说法汇报：「上下文已压缩：23,476 → 2,760 tokens（省下 20,716），用时 44.3s」。
- **`/compact` 之后会把压缩摘要灌进对话**。压缩会往 Claude 的 transcript 里追加一大段摘要，而镜像是按文件长度判断"外部是否写了新内容"的，于是那段摘要会被投影进 dsh 的聊天区。现在任何一次性命令跑完都会把镜像偏移推到当前文件末尾。
- **上下文占用改用 dsh 自己的指示器**。dsh 本来就有一个——发送按钮左边那个圆环，点开还有按 system/tools/messages 分段的明细面板。它的分子来自 `contextPressure` 投影，而那个投影读的正是插件已经写在每条 `assistant/message` 上的 `usage`；缺的只是分母：`contextWindow` 来自 `request/context` 事件，而 Claude 会话从不发起 dsh 模型请求，所以从来没有过这个事件。现在握手拿到实际路由后补一条（`[1m]` 后缀识别为 100 万窗口），dsh 原生的圆环就自己亮了。插件此前自绘的那个圆环连同它的座位一并删掉——它读的两个字段宿主根本没返回过，而且算法用的是**整轮累计**用量（几十次调用相加），所以就算亮了也永远显示 100%。
- **跑轮次时粘的图片会被丢掉**。轮次进行中发的消息走"插话"通道直接折进当前轮，那条路径只取文本，图片既没发给 Claude 也没进转录，待发提示自然也不消失。现在插话同样带上图片。
- **删掉一张图片会把所有待发图片一起删掉**。附件带上每张卡的 ✕ 都接到了「清空整个暂存」，粘两张点掉一张就两张都没了。现在暂存是有序列表，✕ 只删它自己那一张。
- **图片发出去后待发提示不消失**。宿主在开轮时就清空了暂存，但客户端只在 `state.running` 变化时才去问一次，而会话锁定后它已经不再轮询状态了。现在有待发图片时按秒轮询，发出去即清空。

### 新增

- **自动压缩会在对话里说一声**。上下文自动压缩发生在轮次中间、要几十秒，此前 dsh 这边只是安静一阵然后若无其事地继续。现在按终端的做法记一行：「◆ 上下文已自动压缩：183,204 → 12,880 tokens」。

### 变更

- **粘贴的图片改用 dsh 自己的附件带**。此前它落在输入框**下方**（`conversation.input.dock` 之外的那条状态带里），而 dsh 原生的草稿图片在输入框**内部、文本上方**。现在插件遮蔽 `conversation.input.attachments`——就是 dsh 自己那个座位——并复用 dsh 的类名结构，所以位置、尺寸、圆角、hover 删除键与原生完全一致。只有当前会话是 Claude 会话时才遮蔽，dsh 会话保留原生的附件带和拖放蒙层。

## 1.3.0 — 2026-08-20

### 新增

- **上下文占用圆环**。输入框下方显示当前对话占了模型上下文窗口的多少（算法与 waku 一致：最后一次调用的 `input + cache_read + cache_creation + output`，除以该模型的 `contextWindow`），70% 转黄、90% 转红，悬停显示具体 token 数。

### 变更

- **粘贴的图片按 dsh 原生样式预览**。原先只有一行「📷 N 张图片将随下一条消息发送」，现在是 dsh 附件栏同款的 64px 缩略图（16px 圆角、卡内删除键），并且落在输入框**上方**那条独立带里（`conversation.input.dock`），与 dsh 自己的附件栏位置一致。
- **侧栏的 Claude 标记不再需要先点开**。引擎索引此前只统计"活着的 agent"，所以一条 Claude 对话要被打开过才会出现 ✳。现在插件持久化的每会话引擎归属也计入索引，列表里显示出来的 Claude 对话直接就带标记（实测：5 行会话，一条未打开，5 个标记）。
- **导入的对话继承它自己的权限档和模型**。Claude 的 transcript 里记着 `permissionMode` 和 `model`，导入时一并读出来写进会话设置；此前导入的老会话在打开时也会补上。不然导入后会停在默认的「监督 · 每个工具问我 / Claude 默认」，而下一轮就会用一个你没选过的权限档跑起来。

## 1.2.0 — 2026-08-20

彻底解决"消息发出去了、Claude 也在跑，但 dsh web 上就是不显示、刷新也没用"这一类问题。

### 变更

- **补写内容不再自己编轮号**。这是这类故障的总根源。插件此前试过三种编号方式，每种都以不同方式撞上 dsh 的历史装配规则：高位段（1000000+）让 dsh 后续的真实轮次变成倒退序列而被丢弃；挂进已有轮次会让 updates 的 `turn/start` 落在历史窗口之外（`received an update before its start Match`）；自建 turn 1 又会和 dsh agent 的第一轮撞号（同一个轮次出现两个 `turn/start`）。

  现在改成**让 dsh 来开这一轮**：插件把待补写内容排队，用一条内部载体消息唤醒 agent，dsh 分配下一个轮号并进入 pre-step，插件把内容写进这个轮号后直接结束该轮（不调用模型、不消耗额度）。轮号序列完全由 dsh 维护，重号、倒退、start 缺失三种情况都不可能再出现。载体消息不会写进转录，也不会发给 Claude。

- **导入不再截断**。原先超过 240 条只回填最近的部分并打一行"导入截断"说明。现在整段 transcript 全部导入（实测一条 187 条消息的对话完整回填）。

### 修复

- 导入/补播/同步/编号修复四条路径统一走上述新机制。实测：整段重新导入后无 `历史加载失败`、内容完整、刷新后仍在。

## 1.1.5 — 2026-08-20

### 修复

- **满屏的「这次调用没有等到结果（上一次运行被中断）」**。1.1.4 的重复守卫做过了头：它把重复的工具**结果**也一并跳过，于是那些"调用已在日志里、结果还没写进来"的调用永远等不到结果；随后轮次开始时的收尾逻辑会把**整个会话**里所有未应答的调用一次性标红，于是刷出几十行。现在只跳过日志里**已经有结果**的调用结果，未应答的调用照常补上结果；投影也不再对整个会话做收尾——只关闭它自己开出、又确实没等到结果的调用。实测：同一段对话重新导入，红行从 3 条降到 **0 条**。

## 1.1.4 — 2026-08-20

### 修复

- **重复的工具调用会让整段历史加载失败**。补写内容（补播/导入回填）时如果重新发出一个日志里已经存在的 tool-call 编号，dsh 加载历史会直接报错：`tool-call toolu_… received more than one start Match（internal）`，整条会话打不开。1.1.1 之前的补播会重放已渲染过的内容，正是这样制造出重复（在一条会话里查到 21 个）。现在补写前先收集日志里已有的调用编号，**已存在的调用及其结果一律跳过**；本次投影自己写出的编号也会记入，避免同一批里重复。

## 1.1.3 — 2026-08-20

### 修复

- **投影内容会让后续轮次在刷新后消失**。补播/导入原本会自己造一个高位轮号（1000000+）来避免与实时轮次冲突。这是错的：dsh 的实时 agent 用的是**构造时从日志读出的内存计数**，不会跟着高位段走，于是日志里的轮号变成 `1, 2, 3, 1000001, 4, 5, …` 这种**倒退**序列，而 dsh 的历史装配会丢掉倒退的那些轮次——表现就是"发了消息、实时能看到，一刷新就没了/加载不出来"。现在投影**不再新建轮次**：直接挂在日志中编号最高的那个轮次上，用它之后的 step 编号，轮号序列完全交给 dsh 自己维护。已验证：此前丢失的回复恢复显示，刷新后仍在。
- 对已经被旧编号搅乱的会话，插件会在打开时尽量把滞留内容补回；若该会话的轮号序列已多处倒退（无法在 append-only 日志里修复），会在日志里明确说明，并建议重新导入这段 Claude 对话（Claude 侧 transcript 完整，重新导入即可得到干净副本）。

## 1.1.2 — 2026-08-20

### 修复

- **进程被回收后，老对话发消息没反应**。1.1.1 收紧回收阈值后暴露的问题：broker 被回收（或被外部杀掉）时，插件内存里的 run 对象还活着并被下一轮直接复用——它的 reader 在 tail 一个已删除的日志，writer 的 `cat > .../in` 把 fifo 路径重新创建成一个**普通文件**，于是 prompt 写进去就没了，轮次一直等不到回应。现在复用一个 run 之前先 `kill -0` 校验它的 broker 还在；不在就丢弃并按 `--resume` 重新拉起 Claude，上下文照常接上（实测重新拉起后仍能准确复述前两轮内容）。

## 1.1.1 — 2026-08-20

### 修复

- **补播会重放已渲染过的内容**。`attach.json` 里的读取偏移只在优雅 detach 时写，dsh 被直接杀掉（`pm2 restart`）后偏移是陈旧的，下次补播就把已经实时渲染过的一整段又投影一遍：同一批工具调用出现两次，结果与调用跨轮次错配，界面上那张卡片就永远停在「等待输出…」，而真正的回答落在了高位历史段里。现在每个轮次边界（`result`）都会落盘偏移，补播前还会用工具调用 id 与转录已有内容对齐，重叠部分直接跳过。
- **挂机的 Claude 进程回收太慢**。空闲阈值从 6 小时收紧到 45 分钟，扫描周期从 30 分钟改为 10 分钟；dsh 里已不存在的会话（删除的、旧状态残留的）闲置 5 分钟即回收；正在跑轮次的会话不会被扫到。实测一次回收掉 6 个挂机会话、释放约 465MB。上下文不会因此丢失——下次发消息按 `--resume` 重新拉起即可。

## 1.1.0 — 2026-08-20

围绕**导入本机 Claude 对话**和**粘贴图片**两条主线，以及 dsh 重启期间不丢内容。

### 新增

- **粘贴图片**。Claude 会话里粘贴图片不再被 dsh 的「当前模型不支持图片」挡下——那道闸门检查的是会话的 dsh 模型，与 Claude 引擎无关。现在粘贴被插件接管：图片存成 dsh 自己的附件对象（转录里原生显示），原始字节以 stream-json 的 image block 随下一条消息送给 Claude；输入框下方有「📷 N 张图片将随下一条消息发送」的待发提示，可一键清空。实测 Claude 能正确识别所发图片。
- **导入的对话带完整历史**。导入不再只是绑定 Claude 会话 id 后打开一个空会话：整段 transcript 会投影成 dsh 原生事件（用户消息、助手消息、工具卡片、subagent 汇报），标题取 Claude 对话自己的标题。导入的历史写在高位轮号段（1000000+），与真实轮次永不冲突，重启后仍可加载。
- **导入面板搜索**。按标题、首条 prompt 本地过滤，同时对该目录下所有 transcript 做内容 grep；纯内容命中的行会显示命中片段说明为什么匹配。
- **导入面板交互**。预览里的消息渲染成会话气泡，工具调用沿用 dsh 原生卡片；左上角返回箭头回到列表（保留搜索词）；导入过程显示「⏳ 导入中…」，失败在页脚显示原因。
- **分支提醒**。最近两分钟仍在被写入的 Claude 对话，在列表和预览页脚标注「正在使用中」：导入后在 dsh 续聊会让 Claude 会话树分叉，终端 resume 只跟随自己那条分支。
- **dsh 重启期间的内容补播**。dsh 自己被重启（例如对话里执行 `pm2 restart`）会中断它自己的轮次，但 broker 保住了 Claude 进程和输出。重新打开该会话、或该会话开下一轮之前，插件会把断档期间已完成的输出补播进转录（不调用模型、不消耗额度），并推进读取偏移，避免重播。
- **`scripts/safe-restart.sh`**。重启 dsh 前先经 `busy` RPC 确认没有进行中的 Claude 轮次，否则等待（`--wait`）或明确 `--force`。多个 agent 在同一台机器上工作时，盲目重启就是对方对话里 `TOOL_OUTCOME_UNKNOWN` 红卡的来源。

### 变更

- 模型选择器显示**进程实际运行的模型**（取自 Claude init 握手，如 `claude-opus-5[1m]` → `Claude Opus 5 (1M)`），而不是笼统的「跟随 Claude 自己的设置」；显式选过模型时显示所选模型，没跑过的会话回退为「Claude 默认」。
- 导入面板的预览由宿主返回结构化条目（消息/工具分开），不再是拍平的文本行。

### 修复

- 正式 bundle 里遮蔽 dsh 内建座位（模型座、访问模式座、用量 dock）需要显式 `priority: -1`：动态包由 runner 隐式赋负优先级，正式包注册在默认 0 会被单座位插槽拒绝。
- 导入历史曾用 turn 0 写入，触发持久化校验（`turn/end` 轮号必须 ≥ 1）导致会话重启后损坏；改为高位轮号段并写全边界事件。
- 图片附着到 dsh 的 UserMessage 时不再原地 push——那是冻结对象，会以 `object is not extensible` 打断整轮；改为构造副本。
- `fileSize` 对已自带 shell 引用的 transcript 路径二次引用，`stat` 到字面量路径而静默返回 0，导致导入记录的增量偏移为 0。
- 取消看门狗丢失的 `abortedAt` 时间戳（v52 重构时丢的），「中断被无视」的兜底释放因此从未生效。
- `sessions.create` 的兜底参数写错（`{directory}` → `{cwd}`）。
- 断档补播原先只挂在打开会话时；用户重开会话后立刻发消息会绕过它，断档内容被永久跳过。现在开轮之前也会补播。

## 1.0.0 — 2026-08-19

首个版本：把 dsh web 的一个会话交给本机 Claude Code CLI 驱动，全部走 dsh 原生渲染。

- 输入框引擎选择器（DSH | Claude Code），会话与引擎一一绑定且从日志推断，重启不丢。
- Claude 权限档替换 dsh 访问模式选择器；权限请求桥接 dsh 原生审批。
- Claude 模型 / reasoning effort 选择，运行中切换走 `set_model` 控制请求。
- 转录、工具卡片、subagent 嵌套全部写成 dsh durable 事件；工具名映射到 dsh 词汇，`dsh-better-tool-ui` 等插件照常渲染。
- 命令面板并入 Claude 的斜杠命令（只在 Claude 会话出现，撞名加 `-claude` 后缀）；一次性命令带外执行并用面板渲染，`/mcp` 按 TUI 样式画服务器列表。
- 订阅用量读数覆盖 dsh-balance 座位，切回 DSH 自动还原。
- Claude 进程由独立 broker（`setsid`）持有：插件热更新、dsh 重启都不中断正在跑的轮次，重连按字节偏移续读。
